#include "main.h"

/**
  * _abs - compute the absolute value of an integer
  * @n: int type number to be checked
  * Return: absolute value of int
  */
int _abs(int n)
{
	if (n < 0)
		return (-n);
	else
		return (n);
}
